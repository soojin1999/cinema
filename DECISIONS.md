# DECISIONS.md — 이 프로젝트에서 내가 직접 판단한 것들

이 프로젝트는 [Claude Code](https://claude.com/claude-code)와 페어 프로그래밍 방식으로 개발했다. 코드를 같이 짜는 것과 별개로,
**아키텍처 방향을 정하고, 제안된 설계를 검증하고, 원칙을 어긴 부분을 찾아 되돌리는 건 전부 내가 직접 판단했다.**
아래는 그 판단들을 정리한 것이다. 전체 판단 과정과 근거는 [Todo.md](Todo.md)에 결정 로그로, 매 세션의 규칙은
[AGENT.md](AGENT.md)에, 진행 상태는 [STATUS.md](STATUS.md)에 그대로 남아있다.

---

## 아키텍처 방향 판단

### 1. 내가 정한 경계 설계 원칙을, 내가 직접 검증하고 되돌렸다

**상황**: `screening`(당시 이름 `catalog`) 도메인은 상태 전이가 없는 조회 전용이라는 이유로, Controller가
Mapper를 직접 호출하는 구조로 처음 만들어졌다.

**판단**: "지금 MSA 구조를 체험하는 중인데, Facade 없이 직접 조회하는 게 내가 세운 경계 설계 원칙에
위반되는 거 아닌가?"를 직접 캐물었다. 다른 도메인(seat/booking/payment/notification)은 전부 Facade를
통해서만 접근하는데, "Saga에 참여 안 한다"는 이유가 경계 일관성을 깨도 되는 근거는 아니라고 판단했다.

**결과**: `ScreeningFacade`/`ScreeningFacadeImpl`를 신설해서 다른 도메인과 동일한 구조로 통일. Service
계층만(상태 전이 로직이 없으므로) 생략하는 걸로 절충. → [Todo.md: "screening 조회 경계"](Todo.md)

### 2. 프론트엔드 아키텍처 전면 전환을 주도했다

**상황**: 초기엔 Thymeleaf(SSR)로 화면을 붙였다.

**판단**: "이 프로젝트를 `@RestController` 기반으로 구성하고, 클라이언트와는 JSON으로만 데이터를 주고받고
싶다"고 방향을 명확히 재설정했다 — MSA 학습 목적상 서버가 View를 렌더링하는 구조보다 순수 API 서버 구조가
맞다는 판단.

**결과**: Thymeleaf 의존성 제거, 컨트롤러 전체를 `@RestController`로 전환, 공통 예외 처리(`GlobalExceptionHandler`)
신설, 정적 HTML + Vanilla JS 프론트로 재구성. → [docs/frontend.md](docs/frontend.md)

### 3. 코드 레벨 경계를 넘어 데이터 레벨 경계까지 — 더 어려운 선택지를 스스로 골랐다

**상황**: Facade로 코드 레벨 경계는 지켰지만, DB는 단일 스키마에 도메인 간 FK(`booking → schedule_seat` 등)가
그대로 걸려 있는 상태였다.

**판단**: "MSA 구조를 제대로 공부해볼 거면 스키마까지 제대로 분리하는 게 낫겠지?"라며, 더 쉬운 선택지(단일
스키마 유지)가 아니라 더 배우는 게 많은 쪽을 스스로 골랐다. 지금 규모가 작을 때 하는 게 나중(관리자 CRUD가
붙은 뒤)보다 훨씬 싸다는 트레이드오프까지 이해하고 내린 결정.

**검증**: 결정 직후 "스키마를 분리하면 쿼리나 Java 로직도 다 바꿔야 하는 거 아니야?"라고 바로 되물어서,
실제 변경 범위(도메인 간 FK 4개 제거, 기존 SELECT 쿼리는 대부분 그대로)를 확인하고 나서 착수 순서를 정했다.
→ [Todo.md: T-09](Todo.md)

---

## 세부 설계 원칙

### 4. 동시성 제어 전략을 일부러 두 가지 다 구현했다

**상황**: 같은 좌석에 대한 동시 예약 충돌을 막는 방법은 대기형(비관적 락)이든 충돌감지형(낙관적 락)이든
하나만 골라도 기능적으론 충분했다.

**판단**: "왜 이 방식을 쓰고 저 방식은 안 쓰는지"를 몸으로 비교하려고, `SeatService`에 `holdPessimistic`/
`holdOptimistic`을 나란히 구현하기로 결정. 대기시키는 방식과 즉시 충돌 예외를 던지는 방식, 서로 다른 두
실패 모드를 직접 관찰하는 게 목표였다.

**결과**: `schedule_seat`에 낙관적 락용 `version` 컬럼 추가, 요청 파라미터로 락 전략을 선택 가능하게 열어둠.
→ [Todo.md: T-03](Todo.md)

### 5. 보상 트랜잭션 발동 조건과 지연 재조정 정책을 직접 설계했다

**상황**: 결제 실패는 예측 가능한 비즈니스 실패지만, DB 장애 같은 기술적 오류는 예측 밖의 문제라 다르게
다뤄야 했다. 특히 "결제는 성공했는데 confirm 단계가 기술적으로 실패해서 예매가 미확정으로 남는" 애매한
상태가 Saga에서 가장 까다로운 지점이었다.

**판단**: `PaymentFailedException`만 보상(release+cancel) 대상으로 확정하고, 그 외 예측 밖 오류는 보상 없이
그대로 던지기로 함. confirm 실패로 인한 미확정 상태는 즉시 복구하지 않고, 다음 조회 시점에 결제 상태를
다시 확인해서 재조정하는 "지연 재조정" 정책을 세움.

**결과**: `BookingOrchestrator.getResult()`가 조회 시점에 PENDING+SUCCESS 조합을 감지하면 confirm을 자동
재시도. → [Todo.md: "보상 발동 조건", "confirm 단계 실패 시 재조정"](Todo.md)

### 6. dto 설계 기준을 직접 제안하고 프로젝트 전체 원칙으로 확장시켰다

**상황**: 순수 조회 응답 dto까지 전부 불변 `record`로 만들다 보니, MyBatis 매핑 XML이 매번 `<constructor>`
블록으로 장황해졌다.

**판단**: "순수 조회용 dto는 record 말고 Lombok 스타일로 하는 게 어떨까?"라고 먼저 제안했고, 곧이어 "SQL도
지금 컬럼이 같아도 쿼리마다 따로 쓰는 게 확장성 있지 않냐"고 SQL 설계까지 같은 원리로 지적했다.

**결과**: "API 호출부터 응답까지 값을 참조·가공하는 dto는 record, 가공 없이 그대로 리턴되는 응답 dto는
Lombok"이라는 판단 기준을 세워서 `AGENT.md` 규칙으로 명문화. SQL도 컬럼이 같아도 쿼리마다 명시하는
컨벤션으로 확정. → [AGENT.md §1](AGENT.md), [Todo.md: "dto record vs Lombok"](Todo.md)

### 7. 이름 하나도 대충 넘어가지 않았다

**상황**: movie/theater/schedule을 묶는 스키마 이름이 `catalog_db`로 제안됐다.

**판단**: "이 이름만 보고 안에 뭐가 들었는지 한눈에 안 들어온다"고 바로 지적했다.

**결과**: 세 테이블이 실제로 표현하는 업무 개념("상영")을 기준으로, Java 패키지·클래스·XML·DB 스키마 계획명까지
전부 `screening`으로 통일해서 리네이밍했다. → [Todo.md: "catalog → screening 리네이밍"](Todo.md)

---

## 실무 감각

### 8. Git으로 관리하기 전에, 먼저 보안부터 점검했다

**상황**: 로컬에서만 관리하던 프로젝트를 Git으로 버전 관리하고 원격 저장소에 푸시하려던 시점.

**판단**: 바로 커밋하지 않고 "보안상 숨겨야 하는 파일 있어?"를 먼저 물어서 점검부터 했다.

**결과**: 로컬 전용 설정(`.claude/settings.local.json`)과 향후 실제 시크릿이 들어갈 DB 비밀번호를 분리 —
`.env` 파일(git 제외 대상) 하나로 `DB_PASSWORD`를 통일해서, `docker-compose.yml`/`application.yaml`이 서로
다른 값을 갖고 있던 기존 리스크까지 같이 제거했다. → `.gitignore`, `.env.example`

---

더 깊이 보고 싶다면 [Todo.md](Todo.md)(전체 의사결정 로그), [AGENT.md](AGENT.md)(개발 규칙 명세),
[STATUS.md](STATUS.md)(진행 상태)를 참고.
