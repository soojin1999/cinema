# 실제 값이 든 .env 파일은 git에 안 올라간다 (.gitignore 참고).
# 이 파일을 복사해서 .env로 만들고 실제 비밀번호로 채워서 쓴다:
#   cp .env.example .env
#
# DB_PASSWORD는 docker-compose.yml(MYSQL_ROOT_PASSWORD)과 application.yaml(spring.datasource.password)
# 둘 다 같은 값을 봐야 한다 — 이 파일 하나로 통일해서 관리한다.
DB_PASSWORD=password
